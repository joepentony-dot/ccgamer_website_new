name: Easter Egg WARP Validation

on:
  pull_request:
    branches: [main]
    paths:
      - 'resources/css/ccg-overlays.css'
      - 'scripts/test-easter-egg-warp-current.mjs'
      - '.github/workflows/fix-easter-egg-warp-mobile-games.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate-warp:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out current revision
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Record protected hashes
        run: sha256sum index.html resources/css/intro.css js/index-intro.js games/games.json home.html > "$RUNNER_TEMP/warp-protected.sha256"

      - name: Validate WARP integration and test source
        run: |
          node --check js/ccg-global.js
          node --check scripts/test-easter-egg-warp-current.mjs
          grep -q 'triggerWarp' js/ccg-global.js
          test -f resources/css/ccg-overlays.css

      - name: Confirm catalogue integrity
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          games = json.loads(Path('games/games.json').read_text(encoding='utf-8'))
          assert len(games) == 651, len(games)
          assert not any(str(game.get('slug', '')).strip().lower() == 'powerdrome' for game in games)
          print('Catalogue integrity passed: 651 games; Powerdrome absent.')
          PY

      - name: Install browser dependency
        run: npm install --no-save --no-package-lock playwright-core@1.58.2

      - name: Start local website
        run: |
          python -m http.server 4173 --bind 127.0.0.1 > "$RUNNER_TEMP/warp-http.log" 2>&1 &
          for attempt in {1..30}; do curl -sf http://127.0.0.1:4173/home.html >/dev/null && exit 0; sleep 1; done
          cat "$RUNNER_TEMP/warp-http.log"
          exit 1

      - name: Test current WARP presentation
        env:
          CHROMIUM_PATH: /usr/bin/chromium
        run: |
          mkdir -p "$RUNNER_TEMP/warp-shots"
          node scripts/test-easter-egg-warp-current.mjs \
            --base-url http://127.0.0.1:4173/ \
            --output "$RUNNER_TEMP/warp-evidence.json" \
            --report "$RUNNER_TEMP/warp-validation.md" \
            --screenshots "$RUNNER_TEMP/warp-shots"

      - name: Confirm protected files unchanged
        run: sha256sum --check "$RUNNER_TEMP/warp-protected.sha256"

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: easter-egg-warp-validation
          path: |
            ${{ runner.temp }}/warp-evidence.json
            ${{ runner.temp }}/warp-validation.md
            ${{ runner.temp }}/warp-shots
          if-no-files-found: warn
          retention-days: 30

      - name: Remove temporary dependencies
        if: always()
        run: rm -rf node_modules

      - name: Publish summary
        if: always()
        run: test -f "$RUNNER_TEMP/warp-validation.md" && cat "$RUNNER_TEMP/warp-validation.md" >> "$GITHUB_STEP_SUMMARY" || true
