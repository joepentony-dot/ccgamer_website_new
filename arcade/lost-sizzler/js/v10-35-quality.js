/* The Lost Sizzler V10.35 — atlas rendering, sanctuary safety and geometry validation. */
(()=>{
  "use strict";
  if(window.__CCG_LOST_SIZZLER_QUALITY_V135__)return;window.__CCG_LOST_SIZZLER_QUALITY_V135__=true;
  const CELL=64,assets={};
  for(const [key,src] of Object.entries({a:"assets/pixel/enemy-atlas-standard-a-v10-35.png",b:"assets/pixel/enemy-atlas-standard-b-v10-35.png",h:"assets/pixel/enemy-atlas-horde-v10-35.png",environment:"assets/pixel/environment-atlas-v10-35.png"})){const image=new Image();image.src=src;assets[key]=image}
  const key=(x,y)=>`${x},${y}`,dirs=d=>d?.orientation==="horizontal"?[[0,-1],[0,1]]:[[-1,0],[1,0]];
  function clearDoorApproach(x,y){
    if(!world?.map?.[y]||x<=0||y<=0||x>=world.map[0].length-1||y>=world.map.length-1)return false;
    host.blockingDecor=(host.blockingDecor||[]).filter(q=>q.x!==x||q.y!==y);for(const d of world.decor||[])if(d.x===x&&d.y===y&&d.blocking)d.destroyed=true;
    const framed=(world.doorFrameCells||[]).some(q=>q.x===x&&q.y===y);if(world.map[y][x]!==0){world.map[y][x]=0;world.doorRepairCells=world.doorRepairCells||[];world.doorRepairCells.push({x,y,wasFrame:framed})}
    world.doorFrameCells=(world.doorFrameCells||[]).filter(q=>q.x!==x||q.y!==y);return W.walkable(world.map,x,y,host);
  }
  function validateDoorAccess(){
    if(!world||!host)return{checked:0,repaired:0,invalid:0};let checked=0,repaired=0,invalid=0;
    const groups=new Map();for(const d of host.doors||[]){if(d.type==="secret")continue;const id=d.groupId||d.id||`${d.x},${d.y}`;if(!groups.has(id))groups.set(id,[]);groups.get(id).push(d)}
    for(const leaves of groups.values()){checked++;const before=dirs(leaves[0]),sides=before.map(([dx,dy])=>leaves.map(d=>({x:d.x+dx,y:d.y+dy})));for(const side of sides){if(side.some(q=>W.walkable(world.map,q.x,q.y,host)))continue;let fixed=false;for(const q of side)if(clearDoorApproach(q.x,q.y)){repaired++;fixed=true;break}if(!fixed)invalid++}}
    host.doorAccessValidation={checked,repaired,invalid,at:Date.now()};return host.doorAccessValidation;
  }
  function safeEnemyCell(enemy){
    const cells=[];for(const room of world?.rooms||[]){if(room.sanctuary)continue;for(let y=room.y+1;y<room.y+room.h;y++)for(let x=room.x+1;x<room.x+room.w;x++){if(!W.walkable(world.map,x,y,host)||(host.enemies||[]).some(e=>e!==enemy&&e.alive&&e.x===x&&e.y===y)||allPlayers().some(p=>p.health>0&&p.x===x&&p.y===y))continue;cells.push({x,y,d:Math.hypot(x-enemy.x,y-enemy.y)})}}cells.sort((a,b)=>a.d-b.d);return cells[0]||null
  }
  function expelSanctuaryEnemies(){let moved=0;for(const enemy of host?.enemies||[]){if(!enemy?.alive)continue;const room=world.rooms[W.roomAt(world,enemy.x,enemy.y)];if(!room?.sanctuary)continue;const q=safeEnemyCell(enemy);if(q){enemy.x=q.x;enemy.y=q.y;enemy.aiState="idle";enemy.lastSeen=null;enemy.memoryMs=0;moved++}else enemy.alive=false}if(moved)host.revision=(host.revision||0)+1;return moved}
  function regenCell(room){const candidates=[];for(let y=room.y+2;y<room.y+room.h-1;y++)for(let x=room.x+2;x<room.x+room.w-1;x++)if(W.walkable(world.map,x,y,host)&&!(host.blockingDecor||[]).some(q=>q.x===x&&q.y===y))candidates.push({x,y,d:Math.hypot(x-(room.x+room.w/2),y-(room.y+room.h/2))});candidates.sort((a,b)=>a.d-b.d);return candidates[0]}
  function installSanctuaryTiles(){host.sanctuaryRegeneration=[];for(const room of world?.rooms||[]){if(!room.sanctuary)continue;const q=regenCell(room);if(q)host.sanctuaryRegeneration.push({id:`sanctuary-regen-${room.id}`,...q,roomId:room.id,periodMs:3000,accumulators:{}})}}
  function updateSanctuaryRegen(dt){for(const tile of host?.sanctuaryRegeneration||[])for(const p of localPlayers()){const on=p.x===tile.x&&p.y===tile.y&&p.health>0&&p.health<p.maxHealth,current=Number(tile.accumulators[p.id]||0);if(!on){tile.accumulators[p.id]=0;continue}tile.accumulators[p.id]=current+dt;if(tile.accumulators[p.id]<tile.periodMs)continue;tile.accumulators[p.id]-=tile.periodMs;p.health=Math.min(p.maxHealth,p.health+1);p.hpBarMs=2200;try{S.sfx("potion");floatText(p.x,p.y,"SANCTUARY +1 HP",P.green);ring(p.x,p.y,P.green,28);sync()}catch(_){}}}
  function prepareFloor(){validateDoorAccess();expelSanctuaryEnemies();installSanctuaryTiles()}
  if(typeof startWorld==="function"){const old=startWorld;startWorld=function startWorldV135Quality(){const result=old.apply(this,arguments);try{prepareFloor()}catch(error){console.warn("[Lost Sizzler V10.35] floor quality pass failed",error)}return result}}
  if(typeof update==="function"){const old=update;update=function updateV135Quality(dt){const result=old.apply(this,arguments);if(mode==="playing"&&host){try{expelSanctuaryEnemies();updateSanctuaryRegen(Number(dt)||0)}catch(_){}}return result}}
  function drawRegenTiles(){for(const tile of host?.sanctuaryRegeneration||[]){if(!focus||!visibleTo(focus,tile.x,tile.y))continue;const s=ws(tile.x,tile.y),t=performance.now()/500,pulse=.55+.25*Math.sin(t);ctx.save();ctx.globalAlpha=.72;ctx.fillStyle=`rgba(44,194,116,${.18+pulse*.16})`;ctx.fillRect(s.x+3,s.y+3,C.tile-6,C.tile-6);ctx.strokeStyle=P.green;ctx.lineWidth=2;ctx.shadowColor=P.green;ctx.shadowBlur=10+pulse*8;ctx.strokeRect(s.x+5,s.y+5,C.tile-10,C.tile-10);ctx.fillStyle=P.green;ctx.fillRect(s.x+C.tile/2-3,s.y+10,6,C.tile-20);ctx.fillRect(s.x+10,s.y+C.tile/2-3,C.tile-20,6);ctx.restore();if(md(tile,focus)<=1)label("REGENERATION · +1 HP / 3 SEC",s,P.green)}}
  if(typeof drawItems==="function"){const old=drawItems;drawItems=function drawItemsV135Atlas(){const result=old.apply(this,arguments);drawRegenTiles();return result}}

  const enemyRows={scout:["a",0],ambusher:["a",1],hunter:["a",2],guard:["a",3],ghost:["a",4],ranger:["b",0],charger:["b",1],root:["b",2],cook:["b",3],firebreather:["b",4],spider:["h",0],skeleton:["h",1],knight:["h",2],guardian:["h",3],warden:["h",4]};
  function atlasEnemy(e,cx,cy){if(e.follower||e.deathStalker||e.treasureGoblin)return false;const kind=e.hordeWarden?"warden":e.exitWarden?"warden":e.kind,row=enemyRows[kind];if(!row)return false;const image=assets[row[0]];if(!image.complete||!image.naturalWidth)return false;const attacking=(e.meleeSwingMs||0)>0||(e.chargeTelegraphMs||0)>0,walking=e.aiState==="chase"||e.aiState==="search",frame=attacking?4:walking?2+(Math.floor(performance.now()/170)%2):Math.floor(performance.now()/620)%2,facing=(e.facing?.x||1)<0?-1:1;ctx.save();ctx.imageSmoothingEnabled=false;ctx.translate(Math.round(cx),Math.round(cy));ctx.scale(facing,1);ctx.shadowColor=e.hordeWarden?P.red:e.champion?P.gold:"rgba(0,0,0,.55)";ctx.shadowBlur=e.hordeWarden?18:e.champion?10:4;ctx.drawImage(image,frame*CELL,row[1]*CELL,CELL,CELL,-29,-31,58,58);if(e.flash>0){ctx.globalCompositeOperation="screen";ctx.globalAlpha=Math.min(.6,e.flash/200);ctx.fillStyle="#fff";ctx.fillRect(-18,-24,36,43)}ctx.restore();return true}
  if(typeof drawPixelEnemySprite==="function"){const old=drawPixelEnemySprite;drawPixelEnemySprite=function drawPixelEnemySpriteV135(e,cx,cy){if(!atlasEnemy(e,cx,cy))return old.apply(this,arguments)}}
  function envRow(d){return d.sigilGate?3:d.type==="switch"?2:d.type==="bronze"?1:0}
  function overlayAtlasDoors(){const image=assets.environment;if(!image.complete||!image.naturalWidth)return;const t=performance.now();for(const d of host?.doors||[]){if(d.type==="secret"||!visibleTo(focus,d.x,d.y))continue;const duration=Math.max(1,(d.openAt||0)-(d.openingStart||0)),progress=d.open?1:d.opening?Math.max(0,Math.min(1,(t-(d.openingStart||t))/duration)):0,frame=Math.max(0,Math.min(5,Math.round(progress*5))),s=ws(d.x,d.y);ctx.save();ctx.imageSmoothingEnabled=false;ctx.translate(s.x+C.tile/2,s.y+C.tile/2);if(d.orientation!=="horizontal")ctx.rotate(Math.PI/2);ctx.drawImage(image,frame*CELL,envRow(d)*CELL,CELL,CELL,-31,-31,62,62);ctx.restore();if(md(d,focus)<=2){const text=d.sigilGate&&d.locked?"REINFORCED SIGIL GATE":d.locked?(d.type==="switch"?"SWITCH GATE":d.type==="bronze"?"LOCKED BRONZE DOOR":"SEALED DOOR"):d.open?"OPEN DOOR":d.opening?"DOOR OPENING…":"CLOSED DOOR";label(text,{x:s.x,y:s.y-2},d.locked?P.gold:d.open?P.green:P.cyan)}}}
  if(typeof drawDoors==="function"){const old=drawDoors;drawDoors=function drawDoorsV135Atlas(){const result=old.apply(this,arguments);overlayAtlasDoors();return result}}
  function overlayAtlasTorches(){const image=assets.environment;if(!image.complete||!image.naturalWidth)return;const frame=Math.floor(performance.now()/115)%6;for(const l of world?.wallLights||[]){if(!visibleTo(focus,l.x,l.y)&&md(focus,l)>12)continue;const s=ws(l.x,l.y);ctx.save();ctx.imageSmoothingEnabled=false;ctx.drawImage(image,frame*CELL,4*CELL,CELL,CELL,s.x-4,s.y-17,50,50);ctx.restore()}}
  if(typeof drawWallLights==="function"){const old=drawWallLights;drawWallLights=function drawWallLightsV135Atlas(){const result=old.apply(this,arguments);overlayAtlasTorches();return result}}
  if(typeof render==="function"){const old=render;render=function renderV135Copyright(){const result=old.apply(this,arguments);if(mode==="playing"){ctx.save();ctx.font='bold 10px "Courier New",monospace';ctx.textAlign="right";ctx.fillStyle="rgba(250,244,255,.55)";ctx.shadowColor="#030205";ctx.shadowBlur=4;ctx.fillText("© 2026 CHEEKY COMMODORE GAMER",canvas.width-12,canvas.height-10);ctx.restore()}return result}}
  window.CCGLostSizzlerQualityV135={validateDoorAccess,expelSanctuaryEnemies,installSanctuaryTiles,get assets(){return assets}};
})();
