# Lost Sizzler Solo Stabilization Defect Ledger

This ledger tracks the stabilization programme on `codex/lost-sizzler-solo-stabilization`.

Status values: `OPEN`, `INSTRUMENTED`, `FIX IN PROGRESS`, `VERIFYING`, `CLOSED`.

## Active defects

| ID | Status | Defect | Evidence / current finding | Exit criterion |
| --- | --- | --- | --- | --- |
| LS-SOLO-001 | VERIFYING | Long-session Solo simulation can become sluggish and later accelerate | Before the Phase 3 clock repair, uninterrupted live Solo samples ran at only about 0.62x->0.65x active wall time. R59's authoritative RAF loop called `update()` once per accepted RAF and clamped every active frame to at most 45 ms, permanently discarding visible active wall time under sufficiently low FPS. Candidate `ca3ab5e824...` keeps the singleton R59 RAF owner but expands only active Solo Dungeon to bounded wall-time substeps of at most 45 ms each, with an overall 1080 ms / 24-step ceiling per accepted visible RAF. Pause/hidden boundaries still rebase the accepted RAF timestamp and do not create persistent debt. Earlier current-candidate samples include an uninterrupted stabilization baseline of 1.002x and R60 independent Solo samples of 1.02x->1.07x. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` has now completed two full canonical Load Safety executions, including both Solo soak contracts. This moves the timing fix into repeated canonical verification, but the historical long-session defect still needs the explicit sustained-play evidence before closure. | Corrected accelerated soak remains approximately 1.00x before and after lifecycle stress without bursts, and repeated genuinely long-session builds remain approximately 1.00x active wall time through sustained play and pause/visibility transitions. |
| LS-SOLO-002 | OPEN | Multiple periodic runtime ownership systems compete with the frame loop | R59 owns the final RAF callback but also runs `ensure()` every 40 ms. The mode runtime runs its lifecycle `frame()` every 40 ms. R30 owner seal originally checked movement ownership every 16 ms; current branch logic retires that fallback poll once its assignment gate or the broader R30 global guard provides equivalent coverage. R30 global movement guard still runs a 40 ms monitor. The former R32 20 ms global Spy-loader monitor has now been retired: Spy activation is event/lifecycle-driven and the focused Solo monitor diagnostic reports no retained R32 polling timer during ordinary Solo. The former R57 retained bridge was measured during unchanged Solo play re-running R56 ownership eight times in about one second and recording eight R56 cooldown repairs despite no pending chest, trap, shrine, stall or Spy work. Current R57 narrows that bridge to lifecycle dirtiness or pending chest work: a `MutationObserver` watches `data-special-mode`, `data-run-active` and `data-mode-controller`, stable Solo bridge/cooldown deltas are zero, and a single lifecycle mutation drives exactly one bridge pass. R57's 80 ms timer remains for its other stall/contact/timed/Spy responsibilities. The remaining recurring candidates are R30 global movement guard, mode-runtime lifecycle frame and R59 ensure path. | One authoritative Solo simulation/update owner; remaining monitors are passive or narrowly scoped and cannot repeatedly replace Solo frame/update ownership. |
| LS-SOLO-003 | VERIFYING | Pause/focus/visibility transitions may interact with retained timing debt or recovery logic | R59 resets the accepted RAF timestamp at pause boundaries and the bounded Solo clock has no persistent debt accumulator. The previous 16-cycle post-pause ratios were contaminated by observer aliasing: diagnostics sample every 250 ms while the fixture pauses for only 90 ms, so an interval can begin before a pause and be counted wholly as active when the next sample lands after resume. The soak now preserves transition ownership evidence, resets diagnostics after the lifecycle cycles, then measures a fresh uninterrupted active window. R59's independent pause regression remains 1.00 relative cadence across repeated cycles. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` completed two full canonical executions after the 1080 ms / 24-step bounded-clock change and after modal-boundary fixture corrections, including repeated stabilization and long-session soak coverage. | Corrected accelerated soak and repeated long-session coverage show no accumulated pause/hidden debt, burst, slowdown or acceleration; focus/visibility transitions are also verified. |
| LS-SOLO-004 | VERIFYING | Runtime ownership repair activity must remain bounded for stable Solo play | Earlier diagnostics reproduced historical-owner multiplication under top-level-only ownership checks. Current Node ownership coverage reports retained R29/R56/R60 damage layers bounded at three total compatibility layers while deliberately injected external-owner cycles grow only by the injected external layer rather than multiplying R56/R60 layers. Current R60 Solo browser evidence also shows one R60 movement cadence layer, zero R30 ownership repairs, zero owner-seal repairs and a bounded movement chain in healthy samples. Focused canonical diagnostic `v10-41-solo-damage-owner-lifecycle-diagnostic.mjs` records the complete `hurtPlayer` ancestry across repeated pause/focus cycles; exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` completed two full canonical executions with the damage-owner lifecycle, R60 Solo live integrity, stabilization soak and long-session soak all green. Remaining Stage 7 work is to narrow periodic ownership systems while preserving this bounded ancestry. | Stable Solo soak records bounded retained wrapper layers and no unexplained ownership churn/reassertion growth during ordinary play; any repair is event-driven and attributable to a real ownership transition/fault. |
| LS-SOLO-005 | VERIFYING | Canonical load-safety gate can collapse `hurtPlayer` to a callable historical owner that has lost required R56/R60 environmental semantics | Instrumentation reproduced a late Solo state where `hurtPlayer` was still callable but its ancestry had collapsed to the bare historical function, so the old R30 normal-mode invariant could treat it as healthy and retain/restore it. Candidate `1d9096d373...` changes R30 to require currently loaded R56/R60 damage owners in ancestry, prefers the newest captured baseline during recovery, and reinstalls those modern owners after a restore or Spy isolation exit when required. Subsequent work preserves legitimate composed Spy damage ownership, makes the unsupported non-configurable R60 property accessor optional instead of treating it as the authority, and strengthens the transition regression around ancestry semantics. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` now has two full canonical Load Safety passes including damage-owner transition, R60 environmental semantics, Solo live integrity and corrected Solo soak coverage. | Repeated canonical runs recover deliberate bare-owner injection, preserve healthy modern damage ownership during unrelated repairs, and complete R60 environmental semantics plus corrected Solo soak with bounded ancestry and no recurring collapse. |
| LS-SOLO-006 | VERIFYING | Canonical R60 Horde low-FPS perimeter movement can stall before the Solo soak executes | One earlier Canonical Load Safety run stalled the Horde perimeter fixture after injected ~145 ms main-thread stalls. Subsequent clean executions, including exact heads `71893eac3af...` and `766423bb24d...`, passed perimeter movement, projectile cadence, enemy cooldowns and paused-gap isolation. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` has now repeated full canonical Load Safety success twice on the same code, including the R60 Horde low-FPS combat contract. Horde production remains frozen while Solo is stabilized. | Obtain repeated clean runs or a deterministic cause. If intermittent, harden only the regression fixture where justified; if shared timing is responsible, fix the common timing owner without changing Horde gameplay semantics. |
| LS-SOLO-007 | VERIFYING | Solo movement can remain blocked beyond the configured R60 cadence after repeated pause/resume | Healthy instrumented R60 Solo runs show the immediate duplicate movement blocked as intended, followed by a delayed retry with one R60 cadence layer, R30 `goldenMove` unchanged and zero R30/owner-seal repairs. Exact head `766423bb24d...` again preserved that healthy ownership evidence, but the delayed retry happened while the legitimate named-dossier modal had become active (`mode="dossier"`), so movement correctly did not execute. This was fixture modal-state contamination rather than evidence of cadence-owner failure. Candidate `d26bb9aa0c55...` requires any intervening `dossier` state to correspond to a visible dossier, closes only that legitimate modal, verifies the active Solo run returns to `playing`, then gives the cadence a fresh active-play interval before the delayed movement assertion. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` repeated the canonical R60 Solo live and Solo movement coverage twice. | Repeat across subsequent canonical runs; movement must resume after active-play cadence with one R60 layer and zero unexplained ownership repair, while legitimate gameplay modals are explicitly isolated rather than counted as active movement time. |
| LS-SOLO-008 | VERIFYING | Solo Continue can fail to re-enter an active run after Save & Quit | On exact branch head `18ead2c229...`, Canonical Load Safety completed the Floor 1 autosave, Save & Quit, menu return and visible `Continue Solo - Floor 1` path, then timed out for 20 seconds immediately after clicking Continue while waiting for `runActive=true`, `mode=playing`, Floor 1 and rebuilt `world`/`host`/`p1`. The r43 stalled-cleanup diagnostic later showed Continue can restore the saved run and detach a stale remote channel before its remote cleanup promise settles. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` now has two full canonical Load Safety passes after the R43 fixture was corrected to treat a visible named-dossier modal as a legitimate gameplay boundary rather than a failed Continue restore. | Repeated canonical Save & Quit -> Continue cycles restore Floor 1 (and later-floor checkpoints) within the normal lifecycle budget with the original seed/loadout, one-player Solo ownership, rebuilt world/host/player state, no stale menu/pause state and no timeout. |
| LS-SOLO-009 | VERIFYING | Fresh Solo start can fail to reach active `playing` state after the Solo button is clicked | Exact-SHA Load Safety rerun on `ea2628f1...` advanced past syntax and all Node contracts, then failed in `v10-41-r56-playtest-completion.mjs` at the initial post-click Solo activation wait (`runActive=true`, `mode=playing`, `p1` and `host`) after 20 seconds. This is earlier than the previous R60 damage-owner failure and is isolated by dedicated browser contract `v10-41-r55-solo-start-liveness.mjs`, which records body run state, mode/playMode, player/host/run presence, focus/visibility, load-watchdog state, R30/R59 state and recent lifecycle diagnostics on timeout. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` completed two full canonical Load Safety runs with fresh Solo start liveness green. | Repeated canonical fresh Solo starts reach an active one-player run inside the normal lifecycle budget with `runActive=true`, `mode=playing`, valid `p1`/`host`/`run`, no stale pending Solo intent and no recovery-loop dependence; if a timeout recurs, its exact stalled lifecycle stage is identified and repaired. |
| LS-SOLO-010 | VERIFYING | Quick Inventory canonical artwork can lag synchronous inventory reconciliation under historical `sync` ownership | Canonical Chromium on exact head `5d04b24fbc...` showed that calling the then-live historical `syncV125` owner could leave populated quick slots without direct canonical artwork until a later R56 presentation pass. Phase-by-phase tracing later showed `syncV125` could already be live at release-ready before Spy entry, disproving the earlier claim that Spy -> Solo itself was the identified writer. The production Quick Inventory path now owns canonical artwork synchronously, and exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` repeated canonical R56Z diagnostics with immediate populated-slot artwork and no dependence on the retired R56 overlay timer. Cross-mode ownership remains observed for boundedness. | Repeated canonical transitions render populated quick-slot artwork immediately after synchronous reconciliation, retain bounded owner ancestry and do not depend on a later periodic overlay; no Spy/Horde contamination appears. |

## Architecture findings

- `v10-41-r59-live-regression-fixes.js` is the current final RAF owner. Before the stabilization clock candidate, visible active play called `update()` exactly once per accepted RAF with `dt=Math.min(45,gap)`, discarding active wall time above 45 ms per rendered frame under low FPS.
- The current Phase 3 candidate keeps R59 as the singleton RAF owner but gives only `dungeon-solo` bounded wall-time substeps: maximum 45 ms per substep, up to 24 substeps / 1080 ms per accepted visible RAF. It has no persistent debt accumulator, so pause/hidden time remains discarded at the accepted-RAF timestamp boundary. Horde, Spy and other controllers retain their established single-step path.
- The passive Solo diagnostics sample every 250 ms. A short pause entirely between two samples cannot be partitioned accurately by polling alone; if the second sample lands after resume, the whole interval can be counted as active. Lifecycle-stress windows therefore remain useful for ownership/depth evidence but must not be compared directly with uninterrupted wall-time cadence. The canonical soak resets the diagnostics after lifecycle stress before taking the post-stress active timing sample.
- Earlier candidate measurements include an uninterrupted accelerated-soak baseline of 1.002x, R59 independent pause cadence of 30->30 (1.00 relative), and R60 Solo live integrity around 1.02x->1.07x. Exact head `71893eac3af...` was the first recorded all-seven-green canonical head after expansion to the 1080 ms / 24-step visible-gap ceiling and after adding focused damage-owner lifecycle coverage. Exact head `fc396757e376cf472b0b1e81d2c5268fa9688cd2` has now produced two full successful canonical Load Safety executions on the same code. Repeatability is improved, but long-session closure still requires the explicit sustained-play evidence in the defect exit criteria.
- `v10-41-mode-runtime.js` maintains mode lifecycle and owned-system gates from a separate 40 ms interval while also supplying the shared frame boundary.
- `v10-41-r30-owner-seal.js` now prefers an assignment gate and the broader R30 global guard; its 16 ms fallback poll retires once either durable coverage path is active. This removes one continuous normal-mode polling owner from healthy Solo sessions while retaining a fallback for unsupported property-gate environments.
- `v10-41-r30-global-movement-guard.js` maintains update/movement/damage ownership and movement-stall recovery on another 40 ms monitor. Current Phase 4 work treats a callable damage function as unhealthy if required modern R56/R60 environmental owners are absent from ancestry; recovery reinstalls those required owners rather than accepting a bare historical function.
- R60's Solo cadence wrapper calls R30 `adoptReleaseMoveOwner()`. Once accepted, R30 stores that exact function as `goldenMove`; `v10-41-r30-owner-seal.js` then protects/reasserts that function through the assignment gate plus event-driven key checks, with its former 16 ms poll retired when equivalent coverage is present. Current instrumented R60 Solo runs show this stack stable with one R60 cadence layer and no R30/seal repairs during the measured retry.
- `v10-41-r32-spy-loader.js` no longer carries its former always-on 20 ms ordinary-Solo poll. Spy activation is driven by mode/lifecycle observation and actual Spy entry, and the focused R32 Solo diagnostic requires the retained polling timer to remain zero while ordinary Solo is active.
- `v10-41-r57-desktop-prep-stability.js` retires R56's original periodic timer and now narrows the retained R56 bridge to event/pending-driven synchronization. Its 80 ms timer still owns stall/contact/timed/Spy repairs, but stable Solo no longer re-runs the R56 owner bridge or combat maintenance while nothing changes.
- Current damage ancestry can legitimately include marker-neutral or Spy composition wrappers above R60. Ownership validation therefore reasons about complete ancestry and semantics, not merely whether R60 is the top-level function.
- The R60 damage property gate is optional in environments where the existing global `hurtPlayer` descriptor is non-configurable. Correctness cannot depend on that accessor; ancestry-aware composition and lifecycle restoration are the authoritative protection paths.
- The fresh-start failure is treated separately from Save & Quit → Continue: both can end with `runActive` absent, but one originates from a new Solo intent and the other from checkpoint restoration. Dedicated start-liveness instrumentation must identify the stalled owner before any broader recovery loop is added.
- The V10.25 melee/ammo layer installs `syncV125` around the then-current `sync`. Phase-by-phase canonical tracing established that `syncV125` may already be live before Spy starts, so the earlier Spy → Solo writer hypothesis is not retained. Quick Inventory correctness is now verified by synchronous canonical artwork ownership rather than by assuming a particular top-level `sync` identity.
- Named dossier presentation is an intentional gameplay modal: opening it with pause enabled changes `mode` from `playing` to `dossier` and clears input; closing it restores `playing`. Timing and movement fixtures must therefore exclude dossier wall time from active-play cadence measurements instead of treating the modal as a movement failure.
- Ownership/watchdog monitors are not being broadly removed. Consolidation proceeds one owner at a time after sustained timing evidence and targeted regression coverage identify a safe replacement boundary.

## Closed defects

None closed by the stabilization programme yet. Existing historical r29/r30 fixes remain historical and are not reclassified as stabilization closures without fresh verification.

## Verification policy

A short CI smoke test is insufficient to close LS-SOLO-001 or LS-SOLO-003. Long-session timing defects require soak evidence. The target is sustained Solo simulation time approximately equal to active visible wall time, with pause/hidden time excluded and no later catch-up burst. A single all-green canonical head is supporting evidence, not closure; timing and ownership results must repeat before defects are closed.
