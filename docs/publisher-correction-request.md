# Publisher correction request

This temporary branch marker triggers the existing publisher archive pull-request workflow. The workflow applies the requested publisher corrections, regenerates the game and publisher outputs, validates the retired publisher removals, and removes this marker before committing the final branch changes.
